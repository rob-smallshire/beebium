[project]
name = "beebium-icon"
version = "0.1.0"
description = "Packager for the Beebium application icon"
requires-python = ">=3.11"
dependencies = [
    "resvg-py>=0.1.5",
    "pillow>=10.0",
]

[project.scripts]
beebium-icon = "beebium_icon.cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/beebium_icon"]

[tool.pytest.ini_options]
testpaths = ["tests"]
