/**
 * Beebium TypeScript client version.
 *
 * Single source of truth for the client's runtime version string, kept in
 * lockstep with package.json and the rest of the monorepo by bump-my-version.
 */
export const VERSION = "0.1.7";
