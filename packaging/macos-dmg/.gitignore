# Generated background candidates and built disk images are not committed; the
# chosen background is committed explicitly once picked.
backgrounds/
*.dmg
*.tiff
bg-*.png

# ...but the chosen, shipping background IS committed.
!background.tiff
