[project]
name = "beebium-audio-analysis"
version = "0.1.0"
description = "Audio capture and spectral analysis tooling for evaluating Beebium's SN76489 sample playback (issue #82)"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
