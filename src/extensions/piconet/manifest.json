{
    "name": "piconet",
    "display_name": "Piconet",
    "description": "Piconet USB-CDC bridge to a real Econet network",
    "library": "piconet",
    "cli": "piconet",
    "extension_kind": "econet-transport",
    "parameters": [
        {
            "key": "device_path",
            "type": "filepath",
            "description": "Serial device path (e.g. /dev/tty.usbmodem101). Omit or set to 'auto' to discover a connected Piconet by its USB vendor id and a STATUS probe.",
            "required": false
        }
    ]
}
